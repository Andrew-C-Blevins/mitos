# Deferred until M3

Section 7 is intentionally not implemented at this design checkpoint. No SDK,
model ID, prompt builder, AI endpoint, or background model call is present.
Future AI routes may import only authorized reads and the proposals adapter.
Implement the brief's structured wrapper and paid-call guards before enabling any call.
